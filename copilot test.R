# import a toy dataset
data(iris)
# create a new column with the species name 
iris$Species <- as.character(iris$Species)
# create a new column with the species name
iris$Species <- as.character(iris$Species)


